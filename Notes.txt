WIP

character movement
issues:
character bounces when the slope is too steep.
character is uncontrollable when bouncing happens.
character orientation isn't changed while wallriding, not sure if that is preventing movement up the wall.
position reset using _teleportToOrigin should be changed to resetting to last grounded position instead

map:
- there are terrain artifacts which reach great heights.

settings menu
- clearing map cache?

merging scripts?
- number of files might be to much

linux server command:
node /home/oem/Projects/Web/ATSim/maprender/Server/proxy.js