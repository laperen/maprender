WIP

csm shadows
p:
I have the ThreeJS cascading shadow map(csm) example in my instance of ThreeJS, import path "three\examples\jsm\csm". Since the only real shadows being cast are by sun.shadow, will replacing shadowing casting with csm increase performance?
replace current shadow implementation with csm.

performance
p:
When street lamps and aviation obstruction lights are too small for the camera to render, What is a good way to hide them? 

state transition between map creation and ground level roam
p:
Map creation is complete. After generating a map, the user should be able to enter it in a 3rd-person roaming mode.
Add a button to switch to a start location selection phase. In this phase, hide map creation UI and highlight the clicked point with a beacon. The user should be able to return to map creation or proceed to roaming mode.
When transitioning to roaming, hide selection UI, spawn a simple capsule character, and smoothly move/rotate the camera to the character’s 3rd-person point orientation. 
Do not use default orbiting camera, assume a future solution will control roaming mode camera, but do not implement the solution yet.
Physics and Character movement not required yet.
In roaming mode, include a button to return to location selection.

TODO

replace orbit camera?
collision, physics, and character movement